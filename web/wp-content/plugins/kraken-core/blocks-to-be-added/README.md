This folder is for blocks we want to add to the plugin but need to be updated & tested to ensure they work with the plugin structure and theme customization options beforehand.

Currently planning to add the following blocks, in this order:

- Slider + Slider Pagination
- Classic menu + menu toggle + search toggle
- Kraken Event Data (block that outputs event meta for single event template)
- Tabs
- SVG Border
