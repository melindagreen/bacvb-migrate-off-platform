# Search Toggle

Adds a search icon that can be used to toggle the core search block

## Usage

- When toggled the block will add the class ```.is-toggled``` to the target element
- You will need to add your own styling to hide the core search block when/where needed
- The core search block will receive focus when toggled
- Mobile breakpoint setting will use the theme media queries