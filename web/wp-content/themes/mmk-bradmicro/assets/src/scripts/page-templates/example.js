/*
Import the CSS for your template, the slug does not have to match.
*/
import "../../styles/@slug/example.scss";

window.addEventListener("DOMContentLoaded", () => {
    //console.log('example');
});