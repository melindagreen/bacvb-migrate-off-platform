const Save = (props) => {
  const { attributes } = props;
  const { exampleText } = attributes;

  return <p>{exampleText}</p>
}

/*** EXPORTS ****************************************************************/
export default Save;